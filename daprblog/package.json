{
  "name": "dapr-blog-hugo",
  "version": "0.0.1",
  "description": "Blog site for the Dapr project",
  "main": "",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dapr/blog.git"
  },
  "author": "",
  "bugs": {
    "url": "https://github.com/dapr/blog/issues"
  },
  "homepage": "https://dapr.io",
  "scripts": {
    "preinstall": "git -C .. submodule update --init --recursive 2>/dev/null || true",
    "build": "hugo --minify",
    "start": "serve -l ${PORT:-8080} public --no-clipboard"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "postcss-cli": "^11.0.0"
  },
  "dependencies": {
    "hugo-extended": "0.123.3",
    "serve": "^14.2.3"
  }
}
